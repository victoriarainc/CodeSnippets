<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Home Page</title>
    <link rel="stylesheet" href="/css/styles.css">
  </head>
  <body>
    <a href="/logout">Logout</a>
    <a href="/">Home</a>
    <div class="wrapper" id="addWrapper">
      <form class="add" action="/snippets" method="post">
        <label for="text">Title</label>
        <input class='add' id='text' type="text" name="title" value="{{snippet.title}}" placeholder="Title"/>

        <label for="body">Body</label>
        <input class='add' id='body' type="textarea" name="body" value="{{snippet.body}}" placeholder="Body"/>

        <label for="notes">Notes</label>
        <input class='add' id='notes' type="textarea" name="notes" value="{{snippet.notes}}" placeholder="Notes"/>

        <label for="language">Language</label>
        <input class='add' id='language' type="text" name="language" value="{{snippet.language}}" placeholder="Language"/>

        <label for="tags">Tags</label>
          {{#snippet.tags}}
            <input class='add' type="array" id="tag{{@index}}" name="tags[{{@index}}]" value="{{.}}" />
          {{/snippet.tags}}
          <input class='add' type="array" id="tag" name="tags[]" value="" />


        {{#if snippet._id}}
          <input type="hidden" name="_id" value="{{snippet._id}}" />
        {{/if}}

        <span class="delete"><a href="/deleteSnippet/?id={{snippet._id}}">Delete</a></span>

        <button class="addButton" name="add"> + </button>
      </form>
    </div>
  </body>
</html>
